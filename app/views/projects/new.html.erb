
<%= simple_form_for @project do |f| %>

	
	<%= f.input :title %>
	<%= f.input :image %>
	<%= f.input :body %>
	<%= f.input :url %>

	<%= link_to "New Subject", new_subject_path %>

	<p><%= f.button :submit %></p>

<% end %>