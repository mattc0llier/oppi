<h2>New</h2>

<%= simple_form_for @post do |f| %>
<%= hidden_field_tag :project_id, params[:project_id] %>

	<%= f.input :title %>
	<%= f.input :image %>
	<%= f.input :body %>
	<%= f.input :url %>

	<%= f.button :submit %>

<% end %>