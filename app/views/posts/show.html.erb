<h2><%= @post.title %></h2>
	<%= image_tag @post.image.url %>
	<h3><%= @post.body %></h3>
	<h3><%= link_to @post.url %></h3>

	<p> This was posted <%= time_ago_in_words @post.created_at %> ago</p>

	
